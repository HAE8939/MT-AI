---
title: 待测试
description: 当前版本已实现但仍需人工验证的变更项
---

# 待测试

- RunningHub OpenAPI v2 迁移：提交/查询/上传三端点切换 v2（`/openapi/v2/run/workflow/{id}`、`/openapi/v2/query`、`/openapi/v2/media/upload/binary`，Bearer 鉴权），并预置内置模板「Z Image 亿级像素文生图」（workflowId 1997246493079834625，fields 为空按默认参数运行）。**已真实验证**（预览浏览器持真实 Key）：浏览器直连 v2 无 CORS 问题（OPTIONS 预检 200）、上传接口真实成功、提交到达 API 并正确返回业务错误（假 workflowId 报「工作流不存在」）、失败原因透传到任务中心与侧栏状态条。仍需验证：① 真实 workflowId 提交 → 轮询至成功 → 结果图写回画布节点；② 上传中点「取消」应放弃本次提交（不出任务、不跳转、不扣费）；③ 登记弹窗粘贴「导出工作流 API」JSON 解析出 LoadImage/CLIPTextEncode 参数并带默认值。
- 画布侧栏 RunningHub 式运行面板：列表↔运行视图切换、图片双来源、数值步进器、useRunningHubRun 共享 hook。已在浏览器验证：进入/返回视图、空参数工作流（Z Image）只显示说明+运行按钮、本地图上传缩略图与 30MB 拦截、数值步进器带默认值 placeholder、无图画布时下拉显示「画布上还没有图片」、失败任务状态条显示真实错误。仍需验证：① 画布图片节点下拉选择（带小缩略图）与选中节点自动预填第一个图片字段；② 真实出图后状态条显示「已完成，结果已写回画布」、占位节点变成品图；③ 运行弹窗（/workflows 页）数值字段渲染为步进器。
- 智能体模块（减法重构阶段 6）：/agents 页按 图片/视频/文档 分类展示模板；需验证：① 登记一个真实 RunningHub 工作流（须在平台跑通过一次）→ 运行 → 任务中心可见「云工作流」任务 → 结果写回画布节点；② 画布多选节点 →「保存为智能体」→ 智能体页运行插入其他画布，输入槽（空图片节点）可填充重跑；③ 原 7 个内置角色以「文档智能体」出现在文档分类和画布工具栏入口，选中节点执行分析产出文本节点，节点重试正常；④ 旧画布中带 roleId 的文本节点重试会提示找不到模板（字段已改 agentTemplateId，未做迁移，符合未上线约定）。
- Provider 抽象 + 东木-AI 接入（减法重构阶段 2）：渠道新增 provider 声明，配置页「添加东木-AI」按钮，东木渠道模型自动发现与能力分类、生图/视频走异步任务接口；需验证：填入东木 Key 拉取模型后四类模型下拉正确分类、画布文生图/图生图/文生视频在东木渠道可用、取消生成正常、兼容渠道（OpenAI/Gemini/Seedance）原路径不受影响。API 轮询链路已用真实 Key 通过（Seedream 4.5 文生图 12 秒完成）。
- 灵感广场 + 我的（减法重构阶段 5）：新增 /plaza 灵感广场一级页（搜索/分类/分页/详情/复制/收藏，保留 CC BY 4.0 署名与懒加载）、/me 我的页（收藏/我的提示词/素材/生成记录四分区）、/agents 智能体占位页；删除画布内提示词选择弹窗和「提示词存为素材」；需验证：广场收藏后在「我的→收藏」可见且去重正确、我的提示词 CRUD/分组/导入导出正常、素材分区功能与原素材页一致、生成记录可定位画布节点、gallery.json 仍为按需加载不进主包。
- 工作台并入画布（减法重构阶段 4）：删除生图工作台与视频创作台页面、两套生成日志和 Agent 的 4 个 workbench 工具；需验证：导航为 画布/智能体/灵感广场/我的/配置，画布节点可完成文生图、图生图、参考图、局部重绘、视频、TTS 全部场景，本地 Agent 面板工具调用无报错。
- WebDAV 移除（减法重构阶段 3）：删除 WebDAV 同步服务、配置 tab 与 config.json 下发字段；需验证：配置页只剩 渠道/模型/偏好/COS/Codex 五个 tab，站点启动无报错，COS 上传队列不受影响。
- 安全止血（减法重构阶段 0）：COS 默认配置清空并默认关闭、`config.json` 示例渠道与 WebDAV 凭据清空、移除 Vite 内网 WebDAV 代理；需验证：全新浏览器环境打开站点无报错，配置页手动填入 COS 密钥后连接测试与上传队列仍正常，旧浏览器中已保存的用户配置不受影响。
- 腾讯云 COS：配置页新增可编辑 SecretId、SecretKey、Bucket、Region、公网域名和对象前缀，连接测试已确认可上传测试对象；需确认目标 Bucket 的 DELETE CORS 规则，以便自动清理测试对象。
- COS 媒体同步：图片、视频、生成结果与素材本地保存后进入可恢复上传队列，失败会自动重试三次并可在顶部同步中心更换配置后手动重试；需用不同尺寸媒体验证刷新恢复和失败状态。
- 提示词与角色：修复 hydration 初始化异常，提示词库新增 7 条 DMDS 完整专业提示词，角色改用 DMDS 原始 System Prompt；需在新建画布选中节点后验证专业角色工作流。
- DMDS 图片工具：新增双图滑杆对比、360° 全景查看和图片标注；需验证缩放/平移/分隔线、WebGL 资源释放、画笔/矩形/箭头/文字/擦除及生成子节点行为。
- BizyAir 移除（减法重构阶段 1）：删除图纸渲染、双相机多角度、AI 超分三个 BizyAir 工作流及其配置 tab，图片放大只保留本地插值模式，多角度只保留通用模型模式，任务运行时泛化为按 provider 分发的执行器接口；需验证：画布图片工具条无「超分」「图纸渲染」入口，「放大」和「多角度」对话框正常出图，任务中心的画布生图任务徽标/取消/重试/定位正常，配置页无「专业工作流」tab。
- 多角度与全景：多角度保留通用模型模式，全景生成固定单次 2:1 配置；需确认不会修改全局生成偏好。
- 局部重绘：新增矩形自由/1:1/16:9/9:16 选区、羽化半径和附加参考图；需验证原图保持不变且结果生成新节点。
- 专业角色：新增 7 个内置角色及本地增删改恢复，角色可读取选中的文本/图片并输出连接文本节点；需确认不影响全站 Codex Agent 会话。
- Codex 能力清单：新增全部已注册工具的分组说明、任务示例、前置条件和限制；需确认文档侧边栏与 AI 文档索引可正常进入该页面，内容与当前工具行为一致。
- 画布菜单：移除左上角菜单中的“文档”入口；需确认其他菜单项仍正常显示和操作。
- 前端启动：修复模型配置持久化渠道读取时的常量赋值错误，并调整全局 CSS 导入顺序；需运行 `bun run dev` 确认 Vite 不再报告依赖扫描失败和 `@import` 顺序警告。
- 本地 Agent 连接说明：新增提醒说明只有安装 Codex 插件或手动添加 MCP 后才会增加 Codex token 消耗，直接运行 `npx -y @basketikun/canvas-agent` 不会安装 MCP；需验证文案位置清晰。
- 本地 Agent 连接说明：画布面板和配置页 Codex Tab 改为区分“Codex 插件启动画布”和“直接运行 Agent 后网页连接”两种方式，需验证文案和布局清晰。
- 全站 Agent：Agent 面板从画布页抽离为全站常驻面板，挂在全局布局右侧，开/关只挤压左侧内容不遮挡；顶栏和画布工具条各有一个开关按钮，需验证在首页、生图、视频、素材、提示词库、画布等页面都能打开面板并保持 SSE 连接不断开。
- 全站 Agent：新增 `site_navigate` 工具，Agent 可直接跳转 `/`、`/canvas`、`/canvas/:id`、`/image`、`/video`、`/prompts`、`/assets`、`/config`；导航直接执行不走工具确认，需验证跳转正确且面板不消失。
- 全站 Agent：画布工具（canvas_apply_ops 等）仅在当前处于画布页时可用，不在画布页调用会返回“当前不在画布页，请先用 site_navigate 打开画布”，需验证 Agent 会先导航到画布再执行画布操作。
- 全站 Agent：Codex 会话改为单一站点级连续线程（不再按 canvasId 分），跨页面/跨画布保持同一条对话上下文，历史记录即这条线程；本地 Agent workspace 从按画布分改为固定 `site` workspace（`~/.infinite-canvas/codex-workspaces/site`），已装 `@basketikun/canvas-agent` 的用户需更新到新版本才生效。
- 全站 Agent：移除仅前端调用 OpenAI responses 接口的在线画布助手死代码（含 `requestToolResponse`），Agent 统一走 MCP + 本地 Codex 链路。
- 画布节点名称：每个节点保留 `title` 名称字段，默认名称显示在节点上方，双击名称可直接编辑，节点信息弹窗和 JSON 可查看该字段。
- 画布节点工具条：节点顶部工具条改为点击选中节点后显示，单纯鼠标移入 / 移出节点不应再频繁弹出。
- 画布组节点：组内子节点优先响应点击和拖拽；当鼠标位于组内节点上时，应移动 / 编辑该节点，而不是移动整个组。
- 画布组节点：工具栏新增“组”节点，普通节点拖入组区域会高亮目标组，松手后自动吸附进组；拖出组区域会解除归属，拖动组节点会一起移动组内子节点。
- GitHub Pages：新增版本 tag 触发的前端静态站点自动发布 workflow，需在仓库 Pages 设置中选择 GitHub Actions 并验证路由刷新回退。
- 画布图片切图：行数 / 列数会生成可直接拖拽的等分切图线，并支持新增横向 / 纵向线、删除单条线、重置切图线和切片数量预览。
- Codex App 插件文档：新增插件安装教程，区分 AI 自动安装、手动安装和本仓库开发调试安装；同步优化插件 README 与技能说明。
- 本地 Codex 连接画布文档：新增 Canvas Agent、MCP、SSE 与浏览器画布交互原理说明，需确认文档导航和描述是否清晰。
- 顶部导航：缩小桌面顶部导航栏高度，导航项下划线和内容垂直对齐需确认。
- 配置与用户偏好：新增独立顶部导航页面，并在页面和弹窗中复用同一套配置面板；新增 Codex Tab，展示插件安装和本地 Agent 启动步骤，可配置本地 Agent 地址、Connect token 和工具执行确认开关。
- Codex 全局连接：配置页可直接连接本地 Canvas Agent，连接状态同步到画布和顶部 OpenAI 状态图标，刷新后会按本地保存的地址和 token 自动尝试重连；需验证已连接、连接中、连接失败、断开和刷新自动重连状态。
- 画布 Agent：Codex 旧 thread 在重启后不可用时会自动新建会话并重试，需验证进入画布直接发送消息不会再出现 `thread not loaded` 或 `no rollout found`。
- 画布 Agent：切换不同画布时会清空并重新加载当前 canvasId 对应的 Codex 会话，需验证不会继续显示上一个画布的对话。
- 画布 Agent：右侧 Agent 面板移除网站 / 本机模式切换，固定使用本机 Agent。
- 配置与用户偏好：每个模型渠道新增 OpenAI / Gemini 调用格式选择，默认 OpenAI，切换默认格式时同步更新默认 Base URL；渠道 Tab 增加紧凑醒目的提醒，说明模型是否可选需要到“模型”Tab 选择。
- Gemini 调用格式：支持拉取 Gemini 模型列表，并用于文本问答、在线 Agent 工具调用、基础生图和图生图请求。
- Gemini 生图：生图工作台和画布生图会把尺寸配置转换为 Gemini `responseFormat.image.aspectRatio` 传入，需验证 1:1、9:16、16:9 及自定义像素尺寸的输出比例。
- 音频和视频生成：选择 Gemini 调用格式时先给出不支持提示，仍需使用 OpenAI 兼容渠道。
- WebDAV 同步：移除 Next.js 转发代理和连接方式选择，改为浏览器前端直接连接 WebDAV 服务。
- 提示词库：移除 Next.js route 抓取和服务端内存缓存，改为浏览器前端直连 GitHub 原始文件并缓存到 IndexedDB。
- 提示词库：移除已 404 的 `EvoLinkAI/awesome-gpt-image-2-API-and-Prompts` 来源。
- 前端构建：移除 Next.js，改为 Vite + React Router，并将前端目录调整为 `pages` / `components` / `stores` / `lib` / `layouts` / `styles`，需验证首页、图片/视频工作台、素材、提示词库和画布路由跳转。
- 前端构建：修复 TypeScript 严格检查下的构建报错。
- Vercel 部署：新增 `web` 和 `docs` 两个子项目的 Vercel 配置，需分别验证静态前端和文档站部署。
- Docker 部署：运行镜像从 Node serve 改为 nginx 静态托管，需验证前端路由刷新回退。
- 环境变量：移除过期的后端 `.env.example` 示例文件。
- 生图工作台：修复失败图片点击重试后只保存在页面状态、刷新或跳转后丢失的问题，重试成功结果会写入本地图片存储和生成记录。
- 视频生成：任务查询接口直接返回 `url` / `result_url` / `video_url` 时会跳过 `/content` 下载，并优先保存到浏览器本地媒体存储。
- 视频生成：任务创建或查询失败时会解析接口返回的 `msg` / `message` / 嵌套 JSON 错误，并在生成结果失败卡片里展示真实原因。
- 画布生成配置：修复切换文本/视频/音频模式时模型选择仍显示为生图模型（gpt-image-2）的问题，切换模式后应自动匹配对应能力的默认模型。
